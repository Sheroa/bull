{{each data.list as value index}}
	<div class="each">
		<p class="ti"><a href="javascript:void(0);" class="circle"></a><a href="#" class="title">{{value.title}}</a><span>{{value.createTime}}</span></p>
		<p class="cont">{{value.content}}</p>
	</div>
{{/each}}